{
  "name": "api-inventory",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "LOG_LEVEL=trace DEBUG=* NODE_OPTIONS=\"--enable-source-maps --experimental-specifier-resolution=node\" tsx watch --clear-screen=false src/index.ts",
    "dev:debug": "LOG_LEVEL=trace DEBUG=* NODE_OPTIONS=\"--enable-source-maps --experimental-specifier-resolution=node\" tsx watch --inspect=0.0.0.0:9229 --clear-screen=false src/index.ts",
    "stop": "node -e \"const fs=require('fs');const f='.server.pid';if(!fs.existsSync(f)){console.error('No .server.pid found. Is the server running?');process.exit(1)}const pid=Number(fs.readFileSync(f,'utf8').trim());if(!pid){console.error('Invalid PID in .server.pid');process.exit(1)}try{process.kill(pid,'SIGTERM');console.log('Sent SIGTERM to PID',pid);}catch(e){console.error('Failed to signal process',e);process.exit(1)}\"",
    "clean": "node -e \"const fs=require('fs');if(fs.existsSync('dist'))fs.rmSync('dist',{recursive:true,force:true});\"",
    "build": "npm run clean && tsc",
    "start": "NODE_OPTIONS=\"--experimental-specifier-resolution=node\" node dist/index.js",
    "test": "vitest",
    "test:api": "vitest test/e2e",
    "test:run": "vitest run",
    "test:watch": "vitest --watch",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "lint:format": "prettier --check .",
    "env:from-dist": "node scripts/generate-env.mjs",
    "env:prod": "NODE_ENV=production node scripts/generate-env.mjs --out .env",
    "env:test": "NODE_ENV=test node scripts/generate-env.mjs --out .env.test --set PORT=3333 --set STORAGE_ADAPTER=memory --set SQLITE_DB_PATH=./data/inventory.test.db --set NODE_ENV=test",
    "tcr": "bash scripts/tcr.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "2.8.5",
    "dotenv": "17.2.2",
    "express": "5.1.0",
    "uuid": "13.0.0"
  },
  "devDependencies": {
    "@types/cors": "2.8.17",
    "@types/express": "5.0.3",
    "@types/node": "^22.18.13",
    "@types/uuid": "10.0.0",
    "@vitest/coverage-v8": "3.2.4",
    "@vitest/ui": "3.2.4",
    "prettier": "3.3.3",
    "ts-node": "10.9.2",
    "tsx": "4.8.1",
    "typescript": "5.6.3",
    "vitest": "3.2.4"
  }
}
